export async function highlight(code: string) {
    // Stub for Shiki highlighting. 
    // In a real Satori implementation, we would need to map tokens to React styling spans.
    // For this MVP step, we are focusing on the Image Generation infrastructure.
    return code;
}
